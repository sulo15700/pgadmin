apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgadmin-deployment
  labels:
    app: pgadmin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pgadmin
  template:
    metadata:
      app: pgadmin
    spec:
      containers:
        - name: pgadmin-cntr
          image: dpage/pgadmin4
          env:
            - name: PGADMIN_DEFAULT_EMAIL
              value: suleyman@solakedu.com
            - name: PGADMIN_DEFAULT_PASSWORD
              value: C@pe1832
          volumeMounts:
            - mountPath: /var/lib/pgadmin
              name: pgadmin-storage
      volumes:
        - name: pgadmin-storage
          claimName: pgadmin-pv-claim


